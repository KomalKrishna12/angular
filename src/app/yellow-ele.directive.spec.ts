import { YellowEleDirective } from './yellow-ele.directive';

describe('YellowEleDirective', () => {
  it('should create an instance', () => {
    const directive = new YellowEleDirective();
    expect(directive).toBeTruthy();
  });
});
